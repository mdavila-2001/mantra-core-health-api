# Plan — Transparencia de exclusiones, desglose de liquidación y contrato de lotes aseguradora–profesional (Tarea 3 · H8 / MED-E13..E16)

- Fecha: 2026-09-24 · Repos afectados: `mantra-core-health-api` (rama `marcelo/feat-insurance-exclusions-settlement-contracts-api`, worktree en el scratchpad de la sesión) y `mantra-core-health` (rama `marcelo/feat-insurance-exclusions-settlement-contracts`, worktree `C:/wt/front-exclusions`), ambas creadas con `git worktree add … origin/dev` (api `6e672bcd`, front `0cee9882`, verificados iguales a `origin/dev` el 2026-09-24). Modelo (`mantra-core-health-model`): **sin cambios de DDL**; la única pieza de esquema que hace falta (índice único de reclamo por lote) se entrega como handoff en el contrato, no se aplica.
- Predecesor: Tarea 1 (front PR #638 `0cee9882` y #639 en `mockup`; api PR #455 `6e672bcd`), subtarea 2.2 (cláusula y justificación por ítem, patch v4.2.9), subtarea 2.4 (proyección de liquidación del paciente, patch v4.2.11, `docs/tareas/subtarea-2.4-transparencia-copagos/`), Tarea 2 (v4.2.10 + `InsuranceContactChannels`, ya en `dev`), y el `HANDOFF.md` §H8 del plan médico (`AlovidaPromptManager/planes/02-medical-module-plan-b57dfd316c4d/`, copia idéntica en `mantra-core-health/docs/work/`).
- Fuente del requisito: `📋 Registro de procesos por módulo.md` §6.2 «Recepción de solicitudes de órdenes de aprobación» (líneas 589–596 de la copia local: la app responde APROBADO / NO APROBADO **indicando la cláusula del contrato y la excepción de la póliza**) y §2.7 ítem 4 (líneas 350–352: la parte de la aseguradora se factura a la compañía **semanal, quincenal o mensualmente** y el médico saca un informe de lo facturado al seguro, con deducible y sin seguro). El prompt cita «líneas 488–491, Proceso 2, ítems 2.1–2.4»: en la copia local esas líneas son el módulo de análisis médicos; se toma la numeración de la copia local y se registra la discrepancia (ambigüedad A0). HANDOFF §H8 fila «Factura, cobertura y lotes periódicos» (MED-E13–E16) y MATRIX `RP-MED-L0264` (A MEDIAS), `L0265` (BLOQUEADO), `L0266` (A MEDIAS).

## Contexto (qué descubrió la FASE 0)

**Lo que ya está en `dev` y no se reinventa (regla 00 §1.1):**

1. `projectPatientSettlement` (`src/modules/insurance/services/patient-settlement-projection.ts`) ya emite `totalBilledAmount / totalApprovedAmount / totalPatientAmount / totalDeniedAmount`, `result` y `exclusions[]` con los cinco campos que pide CA-3.1 (`claimLineId`, `itemName`, `amount`, `policyClauseReference`, `denialRationale`, más `itemId`), y **ya degrada a `UNDER_REVIEW`** cuando una exclusión no trae cláusula (`:120-135`) — CA-3.3 en lectura. `validateLinkedClaimSettlement` (`linked-claim-validation.ts`) exige por línea `billed = approved + patient + denied`, totales de versión iguales a las sumas y cláusula en toda denegación (422 en escritura). Todo esto **sólo para reclamos con pedido de farmacia o diagnóstico** (`matchesLinkedClaimSnapshot` exige origen PHARMACY/DIAGNOSTIC).
2. Front: la molécula `app-patient-insurance-settlement` ya pinta los cuatro importes y la lista de exclusiones con cláusula y justificación; el detalle `/administration/insurance-claims/:claimId` ya pinta la cláusula como badge y la justificación por ítem (subtarea 2.2) y los tres totales del dictamen.
3. CA-3.4 se cumple **por construcción**: `claim_adjudication_versions`, `claim_line_adjudications` y `patient_explanations_of_benefit` son append-only, no existe PUT/PATCH/DELETE bajo `/insurance-claims/:id/{adjudications,eob}`, re-publicar la misma versión responde `ConflictException` → **409** (`claims.service.ts:506-509`), una corrección crea la versión N+1 con `supersedesVersionId` (`:441-455`) y revertir una versión no vigente falla (`:556-560`).

**Brechas vigentes (verificadas en código, ninguna refutada):**

1. **Reclamo de consulta médica sin contrato.** `claimForMutation` (`claims.service.ts:352-376`) trata todo reclamo sin pedido como «legacy»: `adjudicate` no ejecuta `validateLinkedClaimSettlement` (importes sin conciliar y exclusiones sin cláusula pasan), `publishEob` tampoco, y `PatientSettlementService.forOrders` sólo proyecta por origen PHARMACY/DIAGNOSTIC. `CreateClaimDto` ni siquiera admite `encounterId`: **hoy no existe un camino que cree un reclamo de consulta** (MATRIX L0264). Es lo que H8 declara bloqueado.
2. **No existe el lote periódico al profesional.** `insurance_reconciliation_batches/items` (UC-26-13, `reconciliation.service.ts`) es conciliación manual de pagos por `SECURITY_ADMIN`, con aritmética en coma flotante (`Number(...)`, `.toFixed(2)`, `:112-115`), sin reglas de elegibilidad (estado, EOB, reversión), sin unicidad de reclamo por lote, sin idempotencia y sin resumen de tres importes (L0265 BLOQUEADO, L0266 A MEDIAS).
3. **Detalle del prestador sin desglose conciliado.** `ClaimDetailDto` no expone si la EOB está publicada ni un bloque de liquidación; la pantalla compara facturado vs suma de ítems, pero **no** `facturado = cubierto + copago + rechazado`, y no tiene tabla de exclusiones consolidada con badge de cláusula.
4. **La maqueta miente (kill-test).** `GET /insurance-claims/:id` simulado (`insurance.handlers.ts:929-947`) calcula `totalPatientAmount = billed − approved`: para `CLM-2026-0177` (300.00) da cubierto 150 + copago 150 + rechazado 120 = **420 ≠ 300**. Por línea lo correcto es 150 + 30 + 120 = 300.
5. **El front no verifica la ecuación.** `normalizePatientSettlement` (`patient-insurance-settlement.types.ts`) degrada por campos incompletos, no por descuadre: un `AVAILABLE` con `150/150/120` sobre 300 se mostraría.
6. **CA-3.4 sin evidencia HTTP.** Nada afirma hoy el 409 del re-publicado ni la ausencia de rutas de mutación; y `PreconditionFailedException` responde **422** (`src/common/errors/domain.exception.ts:106-125`), no 412 (ambigüedad A2).
7. No existe `docs/contratos` ni `docs/contracts` en ningún repo; `prompt_tarea_2_aseguradora_marcelo.md` no está en el workspace (Tarea 2 se verificó por código, no por el prompt).
8. Entorno: `gh` 2.97 autenticado (`PabloArauzCaballero`), Docker arriba, DB de integración configurada por `.env` (`DB_*`; valores no leídos). Ninguno de los dos repos tiene los hooks `plan_gate/report_gate` instalados: la disciplina la sostiene este plan.

**Decisiones tomadas para este carril (todas registradas como ambigüedades abajo):** rutas y nombres nuevos en inglés (instrucción del usuario del 2026-09-24); 422 se conserva; el lote se persiste en las tablas de conciliación existentes con unicidad en servicio; los reclamos de consulta médica se **especifican** en el contrato y se **endurecen en lectura**, pero no se crea el camino encuentro→reclamo (es L0264, otro carril).

- Resultado observable: en `/administration/insurance-claims/:claimId` de un reclamo aprobado parcialmente, quien factura ve «Facturado · Cubierto por el seguro · Copago del paciente · Rechazado» que suman al centavo, y una tabla de exclusiones con badge de cláusula y motivo legible; si algo no cuadra o falta la cláusula, la pantalla lo dice en vez de taparlo. El paciente ve el mismo desglose en su pedido. La aseguradora genera un lote periódico (semanal / quincenal / mensual) por profesional que agrupa sólo reclamos adjudicados con EOB publicada y no revertidos, responde el mismo lote si lo pide dos veces y no fabrica ningún comprobante de pago. Ningún endpoint altera una EOB publicada.
- Kill-test: en la maqueta, `CLM-2026-0177` muestra copago 150 y rechazado 120 sobre 300 facturado y nadie lo acusa; contra la API, `POST /practitioner-settlement-batches` responde 404.

## Alcance
- IN (API): `src/modules/insurance/services/claim-settlement-breakdown.ts` (+spec) · `dto/claims-read.dto.ts` (`settlement`) · `services/claims-read.service.ts` (+spec) · `repositories/claim-read.repository.ts` (EOB por versión) · `services/practitioner-settlement-batch.ts` (dominio puro, +spec) · `services/practitioner-settlement-batches.service.ts` (+spec) · `controllers/practitioner-settlement-batches.controller.ts` · `dto/practitioner-settlement-batch.dto.ts` · `repositories/settlement.repository.ts` (consultas nuevas, sin tocar las existentes) · `insurance.concepts.ts` (conceptos nuevos) · `insurance.module.ts` · `controllers/insurance-controllers.spec.ts` (metadatos) · spec de inmutabilidad · `README.md` del módulo · `docs/contracts/insurer-practitioner-settlement-batches.md` · OpenAPI/Postman regenerados · `docs/trabajo/2026-09-24-insurance-exclusions-settlement-contracts/{PLAN,REPORTE}.md` + `evidencia/`.
- IN (front): `core/data-access/insurance/insurance.types.ts`, `insurance.client.ts` (+spec) · `core/data-access/insurance/patient-insurance-settlement.types.ts` (+spec) · `core/money/decimal-strings.ts` (+spec) · `features/insurance/insurance-claim-detail/*` (+spec) · `shared/components/molecules/patient-insurance-settlement/*` (+spec) · `core/mock/handlers/insurance.handlers.ts` (+spec) · `playwright/carril-insurance-exclusions-settlement.spec.ts` · `docs/integrations/backend-api.md` · copia de `PLAN.md`/`REPORTE.md` en `docs/trabajo/2026-09-24-insurance-exclusions-settlement-contracts/`.
- OUT (se anota, no se toca): crear reclamos desde el encuentro / `encounterId` en `CreateClaimDto` (L0264) · proyección al paciente de reclamos de consulta (requiere lo anterior) · pantalla del profesional para el lote y el informe de Contabilidad (L0266; el contrato deja el DTO listo) · `ReconciliationService` existente (coma flotante: se anota como deuda, no se corrige) · patches de modelo (índice único de reclamo por lote: handoff) · `HANDOFF.md`/`MATRIX.md` del plan médico (son de Justin; se enlaza el contrato desde el PR) · `sameDecimal` exportado por `insurance-claim-detail.ts` (se deja; los helpers nuevos viven en `core/money`) · el `Roles` de `BILLING`/`FINANCE` que no existen en `RoleCode` (deuda ya anotada en el front) · QR/cobro/pasarela (regla de contención financiera).
- Ambigüedades registradas:
  - **A0** — Numeración de la fuente: el prompt cita `REGISTRO…md:488-491`; en la copia local el requisito está en 589–596 (§6.2) y 350–352 (§2.7). Se citan ambas. Confirmar con Pablo qué copia numera 488–491.
  - **A1** — Rutas en inglés (instrucción del usuario): el contrato va en `docs/contracts/insurer-practitioner-settlement-batches.md` (el prompt decía `docs/contratos/aseguradora-medico-lotes.md`) y la carpeta de trabajo en `docs/trabajo/2026-09-24-insurance-exclusions-settlement-contracts/` (`docs/trabajo/` se conserva porque es la convención de la regla 20 y de todos los planes previos de ambos repos). Confirmar con Pablo.
  - **A2** — CA-3.4 pide 409 o 412; el proyecto responde 409 (`ConflictException`) y **422** (`PreconditionFailedException`, contrato vigente de toda la API). Supuesto: no se introduce 412; se documenta 409/422 en el contrato. Confirmar con Justin/Pablo.
  - **A3** — Calendario de corte: `DECISIONS.md` del plan médico dice «no definir semana/quincena/mes unilateralmente». Supuesto: la cadencia (`WEEKLY` · `BIWEEKLY` · `MONTHLY`) es un **parámetro obligatorio** que declara la aseguradora al generar, sin valor por defecto; el período se deriva del inicio declarado (semana de 7 días; quincena 1–15 / 16–fin de mes; mes calendario). Confirmar con el propietario del contrato de aseguradora.
  - **A4** — «Médico» en H8 vs tipo de facturador: el lote se agrupa por `providerEntityId` + `billing_provider_type_concept_id`; cubre práctica, farmacia y unidad diagnóstica con el mismo mecanismo. El contrato nombra al profesional y declara la generalización.
  - **A5** — Criterio temporal del corte: se agrupa por **fecha de publicación de la EOB** dentro del período (lo que quedó firme en el período), no por fecha de envío ni de atención. Confirmar con el propietario del contrato.
  - **A6** — Persistencia: se reutilizan `insurance_reconciliation_batches/items` (mismo grano aseguradora × prestador × período) con conceptos de estado nuevos; la unicidad «un reclamo en un solo lote» se garantiza en servicio bajo `pg_advisory_xact_lock` + comprobación, y se propone al dueño del modelo un índice único parcial. Alternativa descartada: tabla nueva (exige DDL ajeno).
  - **A7** — Reclamos sin pedido ni encuentro (backbone UC-26) conservan su comportamiento de escritura; la regla «sin cláusula → en revisión» se aplica a **todo** reclamo en la lectura del detalle (`settlement.availability`). Confirmar si además hay que endurecer la escritura genérica.

## Protocolo de ramas, worktrees y commits
1. Nunca `git switch`/`checkout` en `ALOVIDA/mantra-core-health-api` ni `ALOVIDA/mantra-core-health` (carpetas compartidas entre sesiones). Todo corre desde los worktrees. Antes de cada `git commit`: `git branch --show-current` y `git rev-parse --show-toplevel`.
2. Stagear archivo por archivo (nunca `git add -A`). Commits convencionales: `docs(insurance): formal insurer-practitioner settlement batch contract (H8)` · `feat(insurance): reconciled settlement breakdown with formal clause references` · `feat(insurance): periodic practitioner settlement batches` · `test(e2e): adjudication transparency and copay breakdown` — cada uno con `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
3. Antes de pushear: `git fetch origin dev` y `git rebase origin/dev` (dev se mueve en horas). Primer push con `-u`. PRs a `dev` con `gh pr create --reviewer "jsaldias39,PabloArauzCaballero"`; el merge lo hace una persona.
4. API con Yarn 4 (`yarn add`, nunca `npm`); front con Yarn 4 PnP. `yarn install --immutable` en cada worktree.

## H1 — Contrato canónico aseguradora–profesional (H8 · MED-E13..E16)
**CA:** Dado el handoff H8, cuando el propietario del contrato de aseguradora y Billing abren el documento, entonces encuentran actores, desglose de importes con su ecuación, elegibilidad, calendario semanal/quincenal/mensual, idempotencia, unicidad, inmutabilidad, reversión/cancelación, la prohibición de simular pagos, los endpoints y estados, y la trazabilidad a REGISTRO/HANDOFF/MATRIX, sin ningún supuesto presentado como hecho.
**DoD:** archivo en disco · `grep -c` de las secciones obligatorias = 12 · enlazado desde el README del módulo · `yarn docs:links` no reporta enlaces rotos nuevos.
**Estado:** TODO

### H1.S1 — Documento de contrato
| ID | Microtarea | CA (binario) | DoD (comando → salida esperada) | Estado |
|---|---|---|---|---|
| H1.S1.M1 | `docs/contracts/insurer-practitioner-settlement-batches.md` con secciones: 1 Propósito y trazabilidad (REGISTRO §6.2/§2.7, HANDOFF §H8, MATRIX L0264–L0266) · 2 Actores · 3 Vocabulario de importes (`totalBilledAmount = totalApprovedAmount + totalPatientAmount + totalDeniedAmount`, qué es copago, qué es exclusión, «excluido no es deuda») · 4 Exclusiones formales (cinco campos, cláusula obligatoria, `UNDER_REVIEW`) · 5 Ciclo del reclamo y estados (SUBMITTED → ADJUDICATED → EOB_PUBLISHED → PAID / REVERSED; versiones N+1) · 6 Inmutabilidad y corrección (409/422, nueva versión) · 7 Elegibilidad del lote · 8 Calendario (WEEKLY/BIWEEKLY/MONTHLY, derivación del período, A3/A5) · 9 Idempotencia y unicidad (clave natural, replay, un reclamo en un solo lote, cerrojo, índice propuesto) · 10 Reversión y cancelación después del corte (nota de ajuste en el lote siguiente, nunca edición) · 11 Contención financiera (sin pasarela, sin comprobantes, `totalPaidAmount` nunca escrito por este contrato) · 12 Endpoints, DTOs, roles y resolución de H8 (qué destraba, qué sigue bloqueado: L0264, pantalla del médico) | Las 12 secciones existen y cada supuesto cita su ambigüedad | `grep -c '^## ' docs/contracts/insurer-practitioner-settlement-batches.md` → `12` | HECHO |
| H1.S1.M2 | Sección «Liquidación al profesional (H8)» en `src/modules/insurance/README.md` con enlace al contrato y a los tres endpoints | El README enlaza el contrato | `grep -c 'insurer-practitioner-settlement-batches' src/modules/insurance/README.md` → `≥ 1` | HECHO |

## H2 — API: desglose conciliado y exclusiones formales en el detalle del reclamo (CA-3.1 · CA-3.3 · CA-3.4)
**CA:** Dado un reclamo con dictamen vigente, cuando el prestador consulta `GET /insurance-claims/:id`, entonces recibe `settlement` con los cuatro importes exactos, `reconciled` verdadero sólo si la ecuación cuadra al centavo, `availability` (`AVAILABLE` · `PENDING_PUBLICATION` · `UNDER_REVIEW` · `NOT_AVAILABLE`) y `exclusions[]` con los cinco campos; una exclusión sin cláusula degrada a `UNDER_REVIEW`; re-publicar una EOB responde 409 y ninguna ruta permite editar una adjudicación publicada.
**DoD:** `yarn test src/modules/insurance/services/claim-settlement-breakdown.spec.ts src/modules/insurance/services/claims-read.service.spec.ts src/modules/insurance/services/patient-settlement-projection.spec.ts src/modules/insurance/services/linked-claim-validation.spec.ts` → passed · `yarn typecheck` → 0 · `yarn lint` → 0.
**Estado:** TODO

### H2.S1 — Proyección de lectura
| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H2.S1.M1 | `services/claim-settlement-breakdown.ts`: función pura `buildClaimSettlementBreakdown({ claim, lines, version, adjudications, eob, itemNames })` → `{ availability, totalBilledAmount, totalApprovedAmount, totalPatientAmount, totalDeniedAmount, reconciled, exclusions }` usando `sumarDecimales`/`mismosDecimales` (`src/common/money/decimal-money.ts`); sin versión → `PENDING_PUBLICATION`/`NOT_AVAILABLE`; exclusión sin cláusula, importe nulo o ecuación rota → `UNDER_REVIEW` con importes (para que la pantalla explique) y `reconciled=false`. Spec en tres niveles: correcto (parcial 300 = 150+30+120), límite (escalas distintas `'30.0'`/`'30.00'`, denegado cero, importe > `Number.MAX_SAFE_INTEGER`), inválido (cláusula `' '`, línea sin adjudicación, totales de versión ≠ suma) | La función es determinista y no lanza | `yarn test src/modules/insurance/services/claim-settlement-breakdown.spec.ts` → passed | HECHO |
| H2.S1.M2 | `ClaimReadRepository.findEobsByVersionIds(em, ids)` (lectura simple sobre `PatientExplanationsOfBenefit`); `ClaimSettlementBreakdownDto`/`ClaimExclusionDto` en `dto/claims-read.dto.ts`; `ClaimDetailDto.settlement` y `ClaimDetailDto.eob: { id, publishedAt } \| null`; `ClaimsReadService.getClaim` los llena con `buildClaimSettlementBreakdown` (nombres de ítem por concepto de servicio, como ya resuelve `conceptMap`) | El detalle trae `settlement` y `eob` | `yarn test src/modules/insurance/services/claims-read.service.spec.ts` → passed (caso nuevo «parcial con exclusiones concilia») | HECHO |
| H2.S1.M3 | CA-3.4 con evidencia: spec `services/claims-immutability.spec.ts` (dobles de `EntityManager` como en `claims-linked.service.spec.ts`): (a) `publishEob` con EOB ya existente para la versión → `ConflictException` (409); (b) `adjudicate` tras EOB crea versión N+1 con `supersedesVersionId = N` y no muta la fila N; (c) `reverse` de una versión no vigente → `PreconditionFailedException` (422); (d) en `insurance-controllers.spec.ts`: bajo `ClaimsController` sólo hay `POST` (ningún `PUT/PATCH/DELETE`) | Las cuatro afirmaciones pasan | `yarn test src/modules/insurance/services/claims-immutability.spec.ts src/modules/insurance/controllers/insurance-controllers.spec.ts` → passed | HECHO |
| H2.S1.M4 | Contra la base (regla 80.5.10): `test/integration/patient-coverage-copays.int-spec.ts` gana un `it` «a published EOB cannot be republished (409) and a correction supersedes it» (reutiliza `adjudicate`/`post`/`ownOrders` del archivo). Requiere el PG aislado del spec; si no está disponible se declara en «No cubierto» con el comando exacto | 409 observado por HTTP | `yarn test:integration --testPathPatterns=patient-coverage-copays` → passed (salida en `evidencia/`) | BLOQUEADO |

## H3 — API: lote periódico de liquidación al profesional (CA-3.2)
**CA:** Dado un conjunto de reclamos de un prestador con una aseguradora, cuando la aseguradora pide el corte de un período (semanal / quincenal / mensual), entonces el lote incluye sólo los reclamos `CLAIM_ADJUDICATED` o `CLAIM_PAID` cuya versión vigente tiene EOB publicada dentro del período y sin reversión, excluye `CLAIM_REVERSED` y los que no tienen EOB (`UNDER_REVIEW`/`PENDING_PUBLICATION`), no incluye un reclamo que ya pertenece a otro lote, responde el **mismo** lote si se repite la petición con la misma clave natural, suma con aritmética decimal exacta «Total a transferir por la aseguradora · Total copagos percibidos en consulta · Total exclusiones aplicadas», y no escribe ni expone ningún dato de pago.
**DoD:** `yarn test src/modules/insurance/services/practitioner-settlement-batch.spec.ts src/modules/insurance/services/practitioner-settlement-batches.service.spec.ts src/modules/insurance/controllers/insurance-controllers.spec.ts` → passed · `yarn typecheck` → 0 · `yarn lint` → 0 · OpenAPI/Postman regenerados sin diff pendiente.
**Estado:** TODO

### H3.S1 — Dominio, servicio y contrato HTTP
| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H3.S1.M1 | Conceptos en `insurance.concepts.ts`: `SETTLEMENT_BATCH_ISSUED` («Lote de liquidación emitido»), `SETTLEMENT_ITEM_INCLUDED` («Reclamo incluido en lote»); sin conceptos de pago | Los dos códigos existen con prefijo `insurance` | `grep -c 'SETTLEMENT_BATCH_ISSUED\|SETTLEMENT_ITEM_INCLUDED' src/modules/insurance/insurance.concepts.ts` → `2` | HECHO |
| H3.S1.M2 | `services/practitioner-settlement-batch.ts` (puro): `resolveSettlementPeriod(cadence, periodStart)` → `{ periodStart, periodEnd }` o lanza `PreconditionFailedException` si el inicio no está alineado (quincena que no empieza en 1 ni 16, mes que no empieza en 1); `selectSettlementClaims({ claims, versions, adjudications, eobs, reversals, alreadyBatchedClaimIds, period })` → `{ included[], excluded[{claimId, reason}] , totals }` con `sumarDecimales`. Spec en tres niveles: correcto (dos reclamos dentro del período), límite (EOB en el último segundo del período; quincena de febrero; versión N+1 publicada reemplaza a N), inválido (reversión, sin EOB, ya en lote, moneda distinta → excluido con motivo; inicio desalineado → 422) | Los tres niveles pasan | `yarn test src/modules/insurance/services/practitioner-settlement-batch.spec.ts` → passed | HECHO |
| H3.S1.M3 | `SettlementRepository`: `findBatchByNaturalKey(em, carrierId, providerEntityId, periodStart, periodEnd)`, `findBatchedClaimIds(em, claimIds)`, `findItemsByBatchId`; `services/practitioner-settlement-batches.service.ts`: `generate(dto, actor)` transaccional — `LinkedClaimAccessService.assertInsurer` → `pg_advisory_xact_lock(hashtext(carrier‖provider))` → replay por clave natural (devuelve el existente con `replayed: true`) → carga de reclamos del prestador con esa aseguradora + versiones + adjudicaciones + EOB + reversiones → `selectSettlementClaims` → `createBatch` (`status = SETTLEMENT_BATCH_ISSUED`, `provider_type_concept_id` = tipo de facturador de los reclamos, `total_claimed_amount = billed`, `total_approved_amount = approved`, **`total_paid_amount` nunca se escribe**) + `createItem` por reclamo (`expected_amount = approved`, `status = SETTLEMENT_ITEM_INCLUDED`) → DTO; `getById(id, actor)` y `list(query, actor)` accesibles para la administración de la aseguradora o para el tenant del prestador (prácticas en alcance, mismo puerto que `ClaimsReadService.practiceIdsInScope`); fuera de alcance → 403 con el mismo cuerpo que inexistente. Spec con dobles: replay idempotente, exclusiones, totales decimales, `total_paid_amount` undefined, 403 | Generar dos veces devuelve el mismo `id`; ningún campo de pago se escribe | `yarn test src/modules/insurance/services/practitioner-settlement-batches.service.spec.ts` → passed | HECHO |
| H3.S1.M4 | `dto/practitioner-settlement-batch.dto.ts`: `GeneratePractitionerSettlementBatchDto { insuranceCarrierId, providerEntityId, cadence: 'WEEKLY'\|'BIWEEKLY'\|'MONTHLY', periodStart (date) }`, `PractitionerSettlementBatchDto { id, insuranceCarrierId, carrierName, providerEntityId, cadence, periodStart, periodEnd, currencyCode, status, generatedAt, replayed, totals: { totalBilledAmount, totalApprovedAmount, totalPatientAmount, totalDeniedAmount }, claims: [{ claimId, claimIdentifier, adjudicationVersion, eobPublishedAt, totalBilledAmount, totalApprovedAmount, totalPatientAmount, totalDeniedAmount, exclusionsCount }], excludedClaims: [{ claimId, reason }] }`; `controllers/practitioner-settlement-batches.controller.ts` (`@Controller('practitioner-settlement-batches')`): `POST /` (201 nuevo · 200 replay), `GET /:id`, `GET /?providerEntityId&from&to`; alta en `insurance.module.ts`; `insurance-controllers.spec.ts` afirma rutas, códigos y que el DTO no tiene ninguna propiedad `*paid*`/`*payment*` | Las tres rutas existen y el DTO no expone pagos | `yarn test src/modules/insurance/controllers/insurance-controllers.spec.ts` → passed · `yarn typecheck` → 0 | HECHO |
| H3.S1.M5 | Regenerar contrato: `yarn docs:openapi:generate` y `yarn postman:generate` (los que corre `docs.yml`) y `yarn docs:endpoints:generate` si el pipeline lo exige | CI no falla por diff pendiente | `git status --short openapi docs/postman docs/endpoints` vacío tras regenerar y commitear | BLOQUEADO |

## H4 — Front: desglose de tres importes y tabla de exclusiones en el detalle y en la tarjeta del paciente (CA-3.1 · CA-3.3)
**CA:** Dado un reclamo `PARTIALLY_APPROVED`, cuando quien factura abre `/administration/insurance-claims/:claimId` o el paciente abre su pedido, entonces ve «Cubierto por el seguro · Copago del paciente · Rechazado» que suman el facturado, una tabla de exclusiones con badge de cláusula y texto del motivo, y, si la API manda un descuadre o una exclusión sin cláusula, una alerta que lo dice; ninguna cifra se recalcula en el navegador salvo para **comprobar** la ecuación con aritmética decimal exacta.
**DoD:** `yarn ng test --include='src/app/features/insurance/insurance-claim-detail/**/*.spec.ts' --watch=false` → passed (equivalente PnP de `npx ng test --include=…`) · specs de `patient-insurance-settlement`, `patient-insurance-settlement.types`, `insurance.client`, `insurance.handlers`, `decimal-strings` → passed · `yarn typecheck` → 0 · `yarn lint` sin problemas nuevos en los archivos tocados.
**Estado:** TODO

### H4.S1 — Tipos, pantallas y maqueta
| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H4.S1.M1 | `core/money/decimal-strings.ts`: `addDecimalStrings(values)` y `sameDecimalString(a, b)` sin `Number` (misma técnica que `sameDecimal` del detalle); spec: escala, céntimo, > 2^53 | Suma y compara sin coma flotante | `yarn ng test --include='src/app/core/money/**/*.spec.ts' --watch=false` → passed | TODO |
| H4.S1.M2 | `insurance.types.ts`: `ClaimSettlementBreakdown`, `ClaimExclusion`, `ClaimDetail.settlement`, `ClaimDetail.eob`; `insurance.client.ts`: mapeo del wire (importes se quedan como cadena, `eob.publishedAt` → `Date`); spec del cliente: «getClaim conserva el desglose sin convertir a número» | El cliente no convierte importes | `yarn ng test --include='src/app/core/data-access/insurance/insurance.client.spec.ts' --watch=false` → passed | TODO |
| H4.S1.M3 | `insurance-claim-detail.{html,ts,css}`: sección «Liquidación» (`data-testid="claim-settlement"`) con `dl.claim__facts`: Facturado (`claim-settlement-billed`), Cubierto por el seguro (`claim-settlement-approved`), Copago del paciente (`claim-settlement-patient`), Rechazado (`claim-settlement-denied`) y estado de la EOB; `app-alert tone="warning" data-testid="claim-settlement-mismatch"` si `!settlement.reconciled`; `data-testid="claim-settlement-under-review"` si `availability === 'UNDER_REVIEW'`; tabla `data-testid="claim-exclusions-table"` (Ítem · Monto · Cláusula como `app-badge variant="error" data-testid="claim-exclusion-clause"` · Motivo `data-testid="claim-exclusion-rationale"`), vacía → «Sin exclusiones». Sin desborde a 390 px (`claim__table-scroll` ya existe). Specs: parcial concilia y pinta 2 filas; descuadre muestra la alerta; `UNDER_REVIEW` muestra el aviso y no anuncia disponible | La pantalla muestra y acusa | `yarn ng test --include='src/app/features/insurance/insurance-claim-detail/**/*.spec.ts' --watch=false` → passed | TODO |
| H4.S1.M4 | `normalizePatientSettlement`: además de los campos, exige `totalBilled = approved + patient + denied` y `Σ exclusions.amount = totalDenied` con `decimal-strings`; si falla → `UNDER_REVIEW` y `insuranceSettlement: null`. Spec: 150/150/120 sobre 300 → `UNDER_REVIEW`; 150/30/120 → `AVAILABLE` | Un descuadre nunca se muestra como cargo | `yarn ng test --include='src/app/core/data-access/insurance/patient-insurance-settlement.types.spec.ts' --watch=false` → passed | TODO |
| H4.S1.M5 | Molécula `patient-insurance-settlement.{html,css}`: rótulos «Cubierto por el seguro · Copago a tu cargo · Rechazado por el seguro», `data-testid` en los cuatro importes (`settlement-total-billed/-approved/-patient/-denied`), cada exclusión con `data-testid="settlement-exclusion"`, cláusula como `app-badge variant="error" data-testid="settlement-exclusion-clause"` y motivo `data-testid="settlement-exclusion-rationale"`; spec ajustado | La tarjeta expone badge y testids | `yarn ng test --include='src/app/shared/components/molecules/patient-insurance-settlement/**/*.spec.ts' --watch=false` → passed | TODO |
| H4.S1.M6 | Maqueta `insurance.handlers.ts`: `totalPatientAmount` = Σ (billed − approved) de las líneas aprobadas; `settlement` y `eob` en `GET /insurance-claims/:id` con la misma forma que la API; nueva solicitud sembrada `CLM-2026-0183` (PARTIAL, una línea denegada **sin** cláusula → `settlement.availability = 'UNDER_REVIEW'`) para el nivel inválido; spec `insurance.handlers.spec.ts`: CLM-2026-0177 concilia (300 = 150 + 30 + 120) y CLM-2026-0183 queda en revisión | La maqueta simula los tres niveles | `yarn ng test --include='src/app/core/mock/handlers/insurance.handlers.spec.ts' --watch=false` → passed | TODO |
| H4.S1.M7 | `docs/integrations/backend-api.md`: nota «`GET /insurance-claims/:id` gana `settlement` y `eob`» junto a la de la subtarea 3.2 | La doc describe el contrato vigente | `grep -c 'settlement' docs/integrations/backend-api.md` → `≥ 1` | TODO |

## H5 — E2E Playwright y doble revisión (CA-3.1 en navegador · regla 35)
**CA:** Dado `yarn start` (maqueta) en `localhost:4200`, cuando corre `playwright/carril-insurance-exclusions-settlement.spec.ts` con `--workers=1`, entonces (1) la administración entra y abre `CLM-2026-0177`, (2) ve el badge de cláusula «Cláusula 12.3…» y su motivo, (3) los cuatro importes del DOM cumplen `facturado = cubierto + copago + rechazado` comprobado en el test con aritmética decimal, (4) el paciente entra a `/my-account/diagnostic-orders` y ve el mismo desglose en un pedido `PARTIALLY_APPROVED`, (5) en 1440×900 y 390×844 `document.documentElement.scrollWidth <= innerWidth`, (6) sin errores de consola salvo el ruido CSP de `ng serve`.
**DoD:** `E2E_BASE_URL=http://localhost:4200 npx playwright test playwright/carril-insurance-exclusions-settlement.spec.ts --workers=1` → passed (salida literal en `evidencia/`) · capturas en `evidencia/` · `evidencia/double-review.md` con dos pasadas y nota por pantalla.
**Estado:** TODO

| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H5.S1.M1 | Spec serial con `entrar`/`irA`/`estable` (`playwright/support/sesion.ts`) y actores `admin@alovida.mock` / `paciente@alovida.mock`; helpers en inglés; capturas a `artifacts/playwright/exclusions-settlement/{claim,patient}-{1440,390}.png` | El archivo compila (`yarn typecheck` incluye `playwright/tsconfig.json`) | `yarn typecheck` → 0 | TODO |
| H5.S1.M2 | Levantar `yarn start` en 4200 (o 4210 si está ocupado, ajustando `E2E_BASE_URL`) y correr la suite | 4 tests passed (2 escenarios × 2 viewports) | comando del DoD → `passed`; salida en `evidencia/playwright-exclusions-settlement.txt` | TODO |
| H5.S1.M3 | `evidencia/double-review.md`: primera pasada contra CA-3.1; segunda adversarial con severidad y nota `RECHAZADA` / `ACEPTABLE CON RESERVAS` / `APROBADA` por pantalla; toda `RECHAZADA` se corrige y se recaptura | Ninguna pantalla queda `RECHAZADA` | archivo con las 4 pantallas evaluadas dos veces | TODO |

## H6 — Gates, PRs mergeables y reporte
**CA:** Dado el trabajo terminado, cuando un revisor abre los PRs, entonces cada uno enlaza `PLAN.md`/`REPORTE.md`, está fuera de draft, sin conflictos con `dev`, con typecheck/lint/tests dirigidos pegados, y el reporte abre con el avance calculado.
**DoD:** salidas literales de `gh pr view <n> --json isDraft,mergeable,mergeStateStatus` y `gh pr checks <n>` en `evidencia/`.
**Estado:** TODO

| ID | Microtarea | CA (binario) | DoD | Estado |
|---|---|---|---|---|
| H6.S1.M1 | API: `yarn test src/modules/insurance` · `yarn typecheck` · `yarn lint` | Los tres en 0 | salidas en `evidencia/api-*.txt` | TODO |
| H6.S1.M2 | Front: specs tocados · `yarn typecheck` · `yarn lint` sobre los archivos tocados | Sin rojo ni problemas nuevos | salidas en `evidencia/front-*.txt` | TODO |
| H6.S1.M3 | Commits atómicos, `git fetch origin dev && git rebase origin/dev`, `git push -u origin <rama>` en los dos repos, `gh pr create --base dev … --reviewer "jsaldias39,PabloArauzCaballero"` ×2 con cuerpo que enlaza el contrato, el plan y el reporte, y termina con `🤖 Generated with [Claude Code](https://claude.com/claude-code)` | Dos PRs abiertos contra `dev` | `gh pr view … --json isDraft,mergeable,mergeStateStatus` → `false / MERGEABLE / CLEAN\|UNSTABLE` + `gh pr checks` | TODO |
| H6.S1.M4 | `REPORTE.md` (regla 40, avance en la primera línea) en el API y copia de `PLAN.md`/`REPORTE.md` en `mantra-core-health/docs/trabajo/2026-09-24-insurance-exclusions-settlement-contracts/` | Los dos repos llevan plan y reporte | `ls` de ambas carpetas | TODO |

## Riesgos y bloqueos previstos
| Riesgo | Impacto | Mitigación |
|---|---|---|
| El PG aislado del int-spec de copagos (`mantra-copays-pg`) o Neon no responde desde esta máquina | H2.S1.M4 queda `A MEDIAS`; CA-3.4 sólo con dobles | Declararlo en «No cubierto» con el comando; el 409 queda afirmado en unitario contra el código real del servicio |
| `yarn install` del front (PnP) en worktree nuevo tarda o falla por rutas largas de Windows | Retrasa H4/H5 | Worktree en `C:/wt/front-exclusions` (ruta corta, como los carriles previos); caché global de Yarn |
| Puerto 4200 ocupado por otra sesión | La suite pega en la app equivocada | `yarn start --port 4210` y `E2E_BASE_URL=http://localhost:4210`; registrar el puerto en la evidencia |
| `dev` recibe merges durante el carril | Conflictos en `insurance.handlers.ts`/`claims-read.dto.ts` | `fetch` + `rebase` antes de cada push; nunca forzar |
| La regla «sin cláusula → en revisión» aplicada a todo reclamo en lectura cambia lo que hoy ve la pantalla para reclamos genéricos antiguos | Un reclamo viejo denegado sin cláusula pasa a mostrarse «en revisión» | Es el requisito (REGISTRO §6.2); se avisa en el PR y se registra A7 |
| Reutilizar las tablas de conciliación mezcla dos usos (UC-26-13 manual y el lote nuevo) | Consultas del uso viejo podrían listar lotes nuevos | Estados distintos (`SETTLEMENT_BATCH_ISSUED` vs `RECON_BATCH_OPEN`); el uso viejo filtra por su estado; índice propuesto en el contrato |
