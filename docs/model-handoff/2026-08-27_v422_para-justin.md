# v4.2.2 — tus tres decisiones, hechas · para Justin

**2026-08-27 · de Marcelo.** Respuesta a `RESPUESTA-A-MARCELO-2026-08-27.md`. Las dos decisiones
de modelo están aplicadas a la base viva, y el permiso clínico implementado. Esta nota dice qué
quedó, qué te toca, y tres cosas que aparecieron al hacerlo.

Va acá y no en un PR del esquema porque `SQL/`, `Mantra Core Health Context/` y `salud-db/` no son
repositorios git (defecto B-2): el `.puml`, el DDL regenerado y el patch **sólo existen en la
máquina del modelo**. El patch completo va al final.

---

## 1 · `gap_minutes` — listo. AG-4 destrabado

`scheduling.schedule_rules.gap_minutes`, **integer nullable, sin default**, tal como acordamos.

Ya está en la base viva: columnas de la tabla **14 → 15**, sin default (el patch lo comprueba
explícitamente y rompe si alguien se lo pone).

**De tu lado**, con esto podés cerrar AG-4:

- El paso del generador: `scheduling-catalog.service.ts:357-375`, donde hoy hace
  `const slotMinutes = rule.slotMinutes ?? template.slotMinutes ?? DEFAULT_SLOT_MINUTES;` y avanza
  `cursor + slotMinutes`. El respiro entra ahí con el mismo patrón: `rule.gapMinutes ?? 0`.
- Los DTO: `gapMinutes` en `ScheduleRuleDto` (entrada) y `TemplateRuleDto` (lectura), en
  `scheduling-catalog.dto.ts`.
- La entidad ya lo expone: `ScheduleRules.gapMinutes?: number`.

**Recordá el invariante que vos mismo fijaste en la PR #234:** el paso es `slot + gap`, pero
**cada turno sigue midiendo su duración real**. El respiro es aire del profesional, no un cupo
reservable ni un bloqueo.

> **Sobre tu pedido escrito.** `docs/model-handoff/2026-08-27_gap-minutes-para-marcelo.md` pedía
> `NOT NULL DEFAULT 0`. Lo borro en este mismo PR, siguiendo la convención del buzón (la
> correspondencia se elimina al procesarla), y dejo acá la resolución: el modelo emite `DEFAULT`
> para **una** columna en 1 194 tablas — `row_version`, y la emite el generador porque MikroORM no
> inicializa la propiedad de versión y sin ella todo `INSERT` muere con `23502`. Una segunda
> excepción habría sido deuda de generador permanente a cambio de nada. Ya lo aceptaste en tu
> respuesta; queda escrito para que no se vuelva a preguntar.

---

## 2 · El código de retiro, único por sede — hecho, y con una corrección a tu motivo

`ux_inventory_reservations_pickup_code (pickup_code)` → **`ux_inventory_reservations_pharmacy_site_pickup_code (pharmacy_site_id, pickup_code) WHERE pickup_code IS NOT NULL`**.

Aplicado y **ejercitado** en la base, en transacción revertida:

```
mismo código H7K2QP en la sede A y en la sede B ....... convive        ✅ (antes fallaba)
mismo código dos veces en la MISMA sede .............. ERROR 23505     ✅
dos filas sin código en la misma sede ................ conviven        ✅
```

**Una corrección al razonamiento, no a la decisión.** Escribiste que la unicidad global
«obligaría a coordinar farmacias que no se conocen». Eso valdría si cada farmacia generara su
código, pero **lo genera el backend**, que es una sola base: coordinar no cuesta nada. La razón
que sí aguanta —y la que quedó escrita en el `.puml`— es otra: **con 6 caracteres, la unicidad
global obliga a más reintentos por colisión a medida que crece la red**, y el código se canjea
siempre en UNA sede, así que acotarlo no pierde nada. Tu conclusión es correcta; el argumento
escrito no lo era, y en seis meses alguien iba a leerlo y dudar.

**Se corrigió hoy a propósito.** `inventory_reservations` está en 0 filas: dentro de un mes, con
códigos vivos, el swap dejaría de ser gratis.

⚠️ **Si desplegás esto, el orden importa.** El bootstrap de índices es **ADD-only**: si el código
todavía declara el nombre viejo en `src/orm/catalog/indexes/pharmacy_inventory.idx.ts`, el próximo
arranque en `safe` **re-crea el único global** que el patch borró, reimponiendo en silencio la
regla que descartaste. El tuple del catálogo viaja en el mismo PR que el patch: no los separes.

---

## 3 · La teleconsulta — la nota está; el código es tuyo

Como acordamos en tu segunda respuesta («esto es código y me lo llevo yo»), yo sólo documenté el
modelo. `FK clinical.appointments.channel_concept_id.md` pasó de stub a nota normativa, con:

- **canal = modalidad de atención**, `PRESENCIAL · TELECONSULTA · DOMICILIO`;
- **`NULL` se lee como presencial** — sin backfill, las citas históricas no mienten;
- **conjunto dinámico, dueño la API, sin nota `vs_*`** (la regla de v4.1.9 que evita el problema
  del módulo 64);
- **tu definición tipo-vs-canal**, textual, para que no se vuelva a preguntar.

**Y una advertencia que conviene que veas antes de sembrar:** ya existe `scheduling:channel:*`
(`CH_PORTAL` · `CH_DESK` · `CH_PHONE`) y significa **por qué medio se RESERVÓ** el turno. Tu
columna significa **por qué medio se ATIENDE**. Son dos ejes independientes con el mismo
nombre — una consulta pedida por teléfono puede ser presencial. Nombrá el grupo nuevo «modalidad
de atención» y que esos conceptos **jamás** apunten a esta columna.

Sugerencias de estilo, por si ayudan: la sección va tras los `APPOINTMENT_*` de
`clinical.concepts.ts` (l.635); esa sección usa **displays en inglés** aunque otras del archivo
usen castellano; los codes del módulo son abreviados con prefijo de entidad (`APPT_*`). Y el campo
del DTO entra en `CreateDirectAppointmentDto` entre `durationMinutes` y `reasonText`.

Vi que ya tenés la rama `justin/teleconsulta-canal-de-la-cita` — perfecto.

---

## 4 · El permiso clínico — implementado, tal como lo planteaste

`GET /clinical/patients/:id/summary`: quien atiende pasa **sólo si hoy tiene turno confirmado con
esa persona**. Alcance: **solo `summary`**, como pediste. El titular sigue igual.

Cuatro decisiones de implementación que conviene que conozcas, porque dos se apartan de lo obvio:

**La verdad son tus reservas, no la cita clínica.** Iba a consultar `clinical.appointments` y
encontré que **`APPT_CANCELLED` no se escribe en ningún lado**: una cita cancelada queda
`APPT_BOOKED` para siempre y habría abierto la historia todo el día. Se consulta
`scheduling.appointment_bookings`, que sí refleja el estado real — y de paso trae la zona de la
sede en el mismo `SELECT`. Estados que habilitan: `CONFIRMED`, `CHECKED_IN`, `IN_PROGRESS`,
`COMPLETED` (el que atendió a la mañana sigue necesitando el resumen a la tarde).

**«Hoy» es el día de la sede.** Un turno de las 23:30 en La Paz ya cayó en «mañana» para UTC:
decidir con la fecha del servidor le cerraría la historia al profesional que tiene al paciente
enfrente. Hay un spec que fija exactamente ese borde. Sede sin `time_zone` ⇒ `America/La_Paz`.

**El rechazo es indistinguible.** Sin turno, paciente ajeno y uuid inventado devuelven el mismo
403 con el mismo texto — diferenciarlos le confirmaría a quien probó un uuid que esa persona
existe. Hay spec que lo comprueba comparando los dos mensajes.

**`SUPERADMIN` conserva el bypass.** El `RolesGuard` lo trata como comodín en toda la API y
negárselo acá contradiría al guard que lo dejó entrar; además hay int-specs que dependen de ese
acceso. **Lo dejo como pregunta abierta tuya/del grupo**: si el soporte no debe leer PHI sin
turno, se saca y hay que decidir qué lo reemplaza.

**Lo que NO cubre, dicho claro:** una ruta de veinticinco. `clinical-records.controller.ts` tiene
13 rutas sin comprobación de titularidad, y encounters/observations/orders 8 más. Coincido con tu
posición: eso espera la definición de consentimiento con el cliente, y no se decide entre
nosotros. `clinical_access_grants`, el PDP y las `care_relationships` siguen intactos y
desconectados, a propósito.

**Una prueba del smoke cambió de significado**, y vale la pena mirarla:
`medico.smoke.ts` esperaba `200` en «abre el resumen clínico de su paciente» — pero ese doctor
**no tiene reserva** con ese paciente (su único vínculo era una `care_relationship`, revocada en
el mismo archivo). Ese 200 **era el defecto**. Ahora el caso espera `403` y se llama «guard: sin
turno de hoy no se abre el resumen». Recuperar un camino feliz de verdad exige agendar un turno
en el smoke — media agenda montada; queda como mejora del carril.

---

## 5 · Tres cosas que aparecieron al hacer esto

**Dos índices del modelo viven comentados, y uno importa.**
`SQL/05_profiles/04_indexes.sql:47` tiene `uq_person_account_links_active_user` **comentado**
porque su predicado usa `active_status()`, una función que el modelo nunca definió. Significa que
la base **no garantiza un solo vínculo activo por cuenta**, y `findActiveByUser` devolvería una
fila arbitraria si hubiera dos. Por eso el permiso resuelve la identidad **una sola vez** por
petición y la reutiliza (hay spec que lo fija). El mismo problema tiene
`gist_appointments_practitioner_time` con `held_status()`/`confirmed_status()`. La corrección
canónica existe y es el patrón de v4.0.9: predicado con UUID literales, como
`ux_authentication_credentials_live_password_subject`. **No lo hice** — es su propia tarjeta.

**El modelo nunca resolvió qué significa «cita confirmada».** Ese índice comentado era el único
lugar donde el modelo lo insinuaba, y con funciones inexistentes. Esta regla lo definió por
primera vez, y lo hizo sobre las reservas. Si algún día se quiere en el modelo, es un cambio de
4 capas.

**`docs/data/entity-catalog.md` estaba 49 entidades atrasado** (1 190 → 1 239). Lo regeneré con
`yarn docs:data:sync`, así que mi PR trae ese catch-up además de mi línea. El CI **tiene** el gate
(`git diff --exit-code` sobre `docs/data`, en `docs.yml`) pero sus corridas no concluyen — vale
mirarlo, porque hay más artefactos generados bajo ese mismo gate.

---

## 6 · Estado y cómo verificar

Aplicado a la base viva del stack `mantra-redesa` con `psql -v ON_ERROR_STOP=1`; la sección C del
patch salió en verde.

| Medida | Antes | Después |
|---|---|---|
| Columnas de `schedule_rules` | 14 | **15** |
| `gap_minutes` con DEFAULT | — | **no** (comprobado) |
| Índices totales | 9 183 | **9 183** (±0: −global +compuesto) |
| Índice global viejo | existía | **borrado** |

`yarn typecheck` → 0 · `yarn test src/modules/clinical src/modules/scheduling` → **48 suites /
506 pruebas** en verde (19 del servicio de lectura, 12 de ellas nuevas).

Para reconstruir desde cero no hace falta el patch: sale del DDL generado. Para una base viva,
`SQL/patches/2026-08-27_v422_agenda_gap_y_codigo_por_sede.sql` — **con el código del catálogo ORM
ya desplegado**, por lo del bootstrap ADD-only.

---

## 7 · Lo tuyo, para cerrar el circuito

- **AG-4**: `gap_minutes` te espera.
- **Teleconsulta**: conceptos + DTO + spec, con la advertencia del `scheduling:channel:*`.
- **La auditoría de membresías**: mencionás `AUDITORIA-MEMBRESIAS-DEFAULT-2026-08-27.md` y **no
  está en el workspace** — pasámela y la miro, sobre todo las 5 sin perfil, que sí me tocan.
- **El 422 con el nombre del paciente**: de acuerdo en que es decisión de producto/privacidad.
  Cuando se decida cuánto decir, si toca modelo lo tomo.
- **La rama por defecto**: sigue en `master`. Yo tampoco tengo admin; hay que pedírselo a mdavila
  con las dos evidencias juntas.

---

## 8 · El patch, completo

`SQL/patches/2026-08-27_v422_agenda_gap_y_codigo_por_sede.sql` — va copiado porque esa
carpeta no está en git.

```sql
-- ============================================================================
-- SALUD · patch v4.2.2 (scheduling · el respiro de la franja  ·  pharmacy_inventory ·
-- el código de retiro pasa a ser único POR SEDE) sobre una BD viva
-- Fecha: 2026-08-27
-- Idempotente (ADD COLUMN / CREATE INDEX IF NOT EXISTS · DROP INDEX IF EXISTS).
-- UNA sola pasada: no hay backfill.
--
-- Contexto: gen_ddl.py ya emite la columna y el índice nuevo en SQL/41_scheduling/ y
-- SQL/25_pharmacy_inventory/ desde que los .puml los declaran, así que en un rebuild
-- desde cero este patch NO hace falta. Existe únicamente para una base ya aplicada y
-- poblada. gen_apply.py no escanea SQL/patches/ (solo directorios NN_schema), así que
-- no entra en apply_all.sql.
--
-- QUÉ CIERRA. Las dos decisiones de modelo de Justin del 27/08:
--   A · `scheduling.schedule_rules.gap_minutes` — desbloquea AG-4 (el generador con
--       paso `slot + gap`, la ficha de respiro en Publicar y la vista previa).
--   B · el código de retiro deja de ser único global y pasa a serlo por sede.
--
-- SOBRE A — POR QUÉ NULLABLE Y SIN DEFAULT. El pedido original
-- (`docs/model-handoff/2026-08-27_gap-minutes-para-marcelo.md`) decía
-- `NOT NULL DEFAULT 0`; Justin retiró ese pedido literal al ver que el modelo NO emite
-- defaults —`row_version integer NOT NULL DEFAULT 1` es la única excepción, y la emite
-- gen_ddl.py porque MikroORM no inicializa la propiedad de versión y sin ella todo
-- INSERT muere con 23502—. `NULL` ≡ «sin respiro» dice exactamente lo mismo que 0, y el
-- `?? 0` lo pone el servicio, igual que ya hace con `slot_minutes`. Queda además
-- consistente con su vecina, que también es nullable.
--
-- SOBRE B — POR QUÉ SE CORRIGE UN ÍNDICE DESPLEGADO AYER. v4.2.1 lo creó único GLOBAL.
-- El código se muestra y se canjea siempre en UNA sede: dos farmacias que no se conocen
-- pueden emitir el mismo sin que nadie se confunda, porque nadie va a presentarlo en la
-- otra. Acotarlo achica además el espacio de colisión que el generador debe evitar: con
-- 6 caracteres, la unicidad global obliga a más reintentos a medida que crece la red.
-- Se corrige HOY porque `inventory_reservations` está en 0 filas: dentro de un mes, con
-- códigos vivos, dejaría de ser gratis.
--
-- ORDEN DEL SWAP: se crea el nuevo ANTES de soltar el viejo. El global es MÁS estricto
-- que el compuesto, así que mientras conviven no hay ninguna ventana por la que se cuele
-- un duplicado — al revés sí la habría. Mismo criterio que v4.1.9 §B.
--
-- ⚠️ ACOPLE CON EL CATÁLOGO ORM: el bootstrap de índices es ADD-only (nunca DROP). Si
-- este patch corre contra una base cuyo código todavía declara
-- `ux_inventory_reservations_pickup_code` en `src/orm/catalog/indexes/pharmacy_inventory.idx.ts`,
-- el siguiente arranque en `safe` RE-CREARÍA el único global recién borrado. El cambio
-- del catálogo viaja en el mismo PR que este archivo y debe estar desplegado antes o a
-- la vez. No corras este patch contra un despliegue con el código anterior.
--
-- Este archivo es la salida de gen_ddl.py 41 y 25, no DDL escrito a mano: la columna se
-- declara en `modules/diagram_41_scheduling.puml` y el índice en el `<<INDEX_SET>>` de
-- `modules/diagram_25_pharmacy_inventory.puml`.
--
-- Correlo con `psql -v ON_ERROR_STOP=1 -f`. Con `rebuild_stack.py --yes` no hace falta.
--
-- Delta esperado sobre los conteos canónicos: +0 tablas · +0 FK · **+0 índices netos**
-- (−1 el global, +1 el compuesto) · +1 columna en scheduling.schedule_rules (14 → 15).
-- ============================================================================

-- ---------------------------------------------------------------------------
-- A · El respiro de la franja.
--     (de SQL/41_scheduling/02_tables.sql)
-- ---------------------------------------------------------------------------

ALTER TABLE "scheduling"."schedule_rules"
    ADD COLUMN IF NOT EXISTS "gap_minutes" integer;

-- ---------------------------------------------------------------------------
-- B · El código de retiro, único por sede.
--     (de SQL/25_pharmacy_inventory/04_indexes.sql)
--
-- Sigue siendo PARCIAL por la misma razón que en v4.2.1: la enorme mayoría de las filas
-- —reservas de mostrador y pedidos que aún no llegaron a LISTO_PARA_RETIRO— no tienen
-- código, y Postgres considera cada NULL distinto de los demás, así que un único sin
-- predicado no acotaría nada mientras obliga a razonar sobre NULLs en cada lectura.
-- `pharmacy_site_id` es NOT NULL: la partición nunca es nula.
-- ---------------------------------------------------------------------------

BEGIN;

CREATE UNIQUE INDEX IF NOT EXISTS "ux_inventory_reservations_pharmacy_site_pickup_code"
    ON "pharmacy_inventory"."inventory_reservations" ("pharmacy_site_id", "pickup_code")
    WHERE pickup_code IS NOT NULL;

DROP INDEX IF EXISTS "pharmacy_inventory"."ux_inventory_reservations_pickup_code";

COMMIT;

-- ---------------------------------------------------------------------------
-- C · Comprobación: rompe si el esquema quedó a medias.
-- ---------------------------------------------------------------------------

DO $$
DECLARE
    faltantes text;
BEGIN
    SELECT string_agg(e.objeto, ', ' ORDER BY e.objeto) INTO faltantes
    FROM (VALUES
        ('columna scheduling.schedule_rules.gap_minutes',
            EXISTS (SELECT 1 FROM information_schema.columns
                     WHERE table_schema = 'scheduling' AND table_name = 'schedule_rules'
                       AND column_name = 'gap_minutes')),
        ('índice ux_inventory_reservations_pharmacy_site_pickup_code',
            EXISTS (SELECT 1 FROM pg_indexes WHERE schemaname = 'pharmacy_inventory'
                       AND indexname = 'ux_inventory_reservations_pharmacy_site_pickup_code'))
    ) AS e(objeto, existe)
    WHERE NOT e.existe;

    IF faltantes IS NOT NULL THEN
        RAISE EXCEPTION 'patch v4.2.2 incompleto - faltan: %', faltantes;
    END IF;

    -- La columna no debe llevar default: el modelo no emite ninguno salvo row_version.
    IF EXISTS (SELECT 1 FROM information_schema.columns
                WHERE table_schema = 'scheduling' AND table_name = 'schedule_rules'
                  AND column_name = 'gap_minutes'
                  AND column_default IS NOT NULL) THEN
        RAISE EXCEPTION 'patch v4.2.2 incorrecto - gap_minutes tiene DEFAULT y no debería';
    END IF;

    -- El viejo tiene que haber desaparecido: si sobrevive, la unicidad sigue siendo
    -- global y la decisión de producto no se aplicó.
    IF EXISTS (SELECT 1 FROM pg_indexes WHERE schemaname = 'pharmacy_inventory'
                 AND indexname = 'ux_inventory_reservations_pickup_code') THEN
        RAISE EXCEPTION 'patch v4.2.2 incompleto - ux_inventory_reservations_pickup_code sigue existiendo';
    END IF;

    -- El nuevo tiene que ser PARCIAL y compuesto: un único total sobre las dos columnas
    -- sería otra regla, no un índice peor.
    IF NOT EXISTS (SELECT 1 FROM pg_indexes
                    WHERE schemaname = 'pharmacy_inventory'
                      AND indexname = 'ux_inventory_reservations_pharmacy_site_pickup_code'
                      AND indexdef ILIKE '%WHERE%pickup_code IS NOT NULL%'
                      AND indexdef ILIKE '%pharmacy_site_id%') THEN
        RAISE EXCEPTION 'patch v4.2.2 incompleto - el índice nuevo no es parcial o no incluye pharmacy_site_id';
    END IF;

    RAISE NOTICE 'patch v4.2.2 · C: esquema completo (1 columna · swap de único a por-sede)';
END $$;
```
