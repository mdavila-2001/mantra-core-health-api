# compiled output
/dist
/node_modules
/build
/.claude

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# temp directory
.temp
.tmp

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# graphify — grafo generado (artefacto local, se regenera con `graphify update .`).
# No se versiona. Anclado a la raíz; el de src se ignora aparte.
/graphify-out/
src/graphify-out/

# Claude Code — configuración y skills locales por usuario. Cada desarrollador usa
# las suyas; no se versionan para no chocar con las de otros devs.
.claude/
CLAUDE.md

# Caché de metadata de MikroORM (valor por defecto: <cwd>/temp)
/temp

# database/ y docs/ de la RAÍZ — documentación y artefactos generados, no código.
# No se versionan (viven en local). Anclados con "/" inicial para que la regla NO
# ignore por accidente otras carpetas del mismo nombre en el árbol (p. ej. src/database).
/database/
/docs/
# Cachés de Python (toolkit de generación).
*.pyc

# Documentación/contrato generados (misma razón que /database/ y /docs/ arriba):
# se regeneran con `yarn docs:openapi:generate`/`yarn docs:build`, no se versionan.
# El workflow de CI que los construía y validaba tampoco se versiona con ellos —
# un check permanentemente roto (sin su contenido en un checkout limpio) es peor
# que no tenerlo.
/openapi/
/asyncapi/
/mkdocs.yml
/redocly.yaml
/structurizr/
/.github/workflows/docs.yml

# prompt 
CLAUDE.md
/coverage-integration

# Salidas del smoke test (artefactos generados)
/output.smoke.test.json
/output.smoke.test.csv
site/
dist-docs/
